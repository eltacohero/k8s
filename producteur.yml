apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: producteur
spec:
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: alpine
        image: alpine:3.12
        command: ["/bin/sh", "-c"]
        args: ["echo $HOSTNAME $(date) > /web/index.html; sleep 60"]  
        volumeMounts:
          - name: web
            mountpath: /web
      volumes:
      - name: web
        hostPath:
          path: /
