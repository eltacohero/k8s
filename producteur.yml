apiVersion: v1
kind: DaemonSet
metadata:
  name: producteur
spec:
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: alpine
        image: alpine

